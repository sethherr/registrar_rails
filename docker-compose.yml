# Generated by Cloud66 Starter
---
custom_web:
  build: .
  command: bundle exec puma -e _env:RACK_ENV -b unix:///tmp/web_server.sock --pidfile /tmp/web_server.pid -d
  ports:
  - "3000:80"
  links:
  - postgresql
  - postgresql:postgresql.cloud66.local
  - redis
  - redis:redis.cloud66.local
  environment:
  - RAILS_ENV=production
  - RACK_ENV=production
worker_critical:
  build: .
  command: bundle exec sidekiq -q default -q mailers
  links:
  - postgresql
  - postgresql:postgresql.cloud66.local
  - redis
  - redis:redis.cloud66.local
  environment:
  - RAILS_ENV=production
  - RACK_ENV=production
postgresql:
  image: postgresql
redis:
  image: redis
